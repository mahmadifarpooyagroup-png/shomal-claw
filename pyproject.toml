[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "atrin"
version = "0.1.0"
description = "Atrin Smart Service Platform"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["atrin*"]
